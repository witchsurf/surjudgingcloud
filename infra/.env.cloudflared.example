# Cloudflare Tunnel token for the named tunnel that publishes the HP mini PC stack.
# Create the tunnel in Cloudflare Zero Trust, then copy the Docker token here.
CLOUDFLARE_TUNNEL_TOKEN=replace-with-your-cloudflare-tunnel-token
